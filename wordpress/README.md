# WordPress theme (live website)

This is the Forge Nexxus theme deployed to Cloudways app `ghxskkxdmf` at https://forgenexxus.com.

- Marketing pages use the WordPress templates (`front-page.php`, `page-pricing.php`, legal pages).
- `/app/` is a carbon copy of the GitHub `docs/` client: `page-app.php` plus `assets/js/forge-*.js`, `onboarding.js`, `assets/css/app.css`, and `auth.css`.

See `SOURCE.txt` for the GitHub commit that was copied. Restore the pre-copy theme from `backups/wordpress-2026-09-20-pre-app-sync/`.
