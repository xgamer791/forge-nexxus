// The only house rule left in this file. Everything about how a page looks --
// what opens it, how it is laid out, its palette, its type, what it covers --
// belongs to the design files, not here. The platform contract in
// `generate.ts` owns format, storage and serving, and nothing else.
export const FORGE_MD = "# Forge — standing rules for the website agent\n\n## Rebuild means a different design\n\nA rebuild keeps the onboarding answers and throws away the page they produced. The same answers must not produce the same website twice: design this one from the business itself rather than from what you built before, and do not carry forward the previous page's HTML, CSS, composition, palette, type or section treatment. Changing the words or swapping the pictures is not a new design.\n\nHow the new page is designed is yours to decide, exactly as on any other build. Nothing here prescribes an opening, a layout, a palette or a type treatment — being different from the discarded page is the only requirement a rebuild adds.\n";
